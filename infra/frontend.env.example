# Frontend Environment Variables Example

# This file contains example environment variables for the React frontend application.
# You can copy this file to `.env` and fill in the required values.

# API_URL is the base URL for the backend API
REACT_APP_API_URL=http://localhost:8000/api/v1

# JWT_SECRET is the secret used for JWT token signing (should be kept secret)
REACT_APP_JWT_SECRET=your_jwt_secret_here

# Other environment variables can be added as needed
# For example, you might want to add variables for feature flags or third-party service keys.